name: finix
description: Otizm terapisi için kart uygulaması. Oyun, veri kaydı ve raporlama işlevlerini içerir.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # === 📦 VERİ YÖNETİMİ VE DURUM (HIVE ÖNCELİKLİ) ===
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  provider: ^6.1.2
  uuid: ^4.5.1
  shared_preferences: ^2.2.2

  # === 🎙️ MEDYA, SES VE İZİN YÖNETİMİ ===
  flutter_sound: ^9.2.13
  path_provider: ^2.1.2
  image_picker: ^1.1.0
  permission_handler: ^11.3.1
  flutter_tts: ^3.8.5   # Text-to-Speech (Terapilerde sesli yönlendirme için)

  # === 📊 GRAFİK VE İSTATİSTİK ARAÇLARI ===
  fl_chart: 1.1.0
  intl: ^0.19.0
  path: ^1.9.0

  # === 🎨 UI/UX GELİŞTİRMELERİ ===
  google_fonts: ^6.2.1
  flutter_slidable: ^3.0.1
  cached_network_image: ^3.3.1

  # === 🧾 RAPORLAMA VE ÇIKTI ===
  pdf: ^3.11.1
  printing: ^5.13.4

  # === 🪲 HATA YÖNETİMİ VE STABİLİTE ===
  firebase_core: ^2.27.0
  firebase_crashlytics: ^3.4.19

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
  build_runner: 2.4.13     # ✅ UYUMLU SÜRÜM
  hive_generator: ^2.0.1
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.1

dependency_overrides:
  vector_math: 2.2.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/meslekler/
    - assets/nesneler/
    - assets/tasitlar/

# === 🔖 Uygulama İkonu ===
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/logo.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/logo.png"

# === 💡 Splash Ekranı ===
flutter_native_splash:
  color: "#0A0A0C"
  image: assets/logo.png
  android_12:
    color: "#0A0A0C"
    image: assets/logo.png
  web: false
